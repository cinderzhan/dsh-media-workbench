修复源码 link: 安装导致 Desktop 启动失败。

当源码目录位于 Profile 外部，Node 按源码真实路径解析宿主依赖，无法找到 @deepseek-ai/schemastery / @deepseek-ai/dsh-tools。现在正常解析失败时从 DSH_HOME 的 Profile 解析宿主单例，不安装第二份 Harness，也不会掩盖依赖自身执行错误。

验证：复现原始模块缺失；增加外部源码目录解析回归测试；使用同样 link: 路径启动隔离 Harness；实际重启本机 DSH Desktop 0.8.1，恢复正常主界面并打开内容运营工作台、确认选题输入框可用。

修复页 npm HTTP404 是该私有插件未在 npm 发布的市场查询结果，不是本次模块加载失败的根因。使用本仓库源码或 Release 更新，不要按 npm 包名寻找市场版本。
