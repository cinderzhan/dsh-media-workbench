选题、达人、Campaign 和数据采集改为紧凑表格。

- 选题默认仅展示名称、彩色状态和关联人，输入名称后回车创建，详细操作通过名称打开。
- 数据采集增加官方／达人筛选和 24h／72h／至今切换，指标按列展示，手动录入继续追加快照。
- 数据监控支持自由勾选多条内容，使用折线图或分组柱状图比较平台与时期；缺失值不计零，至今使用最近快照。

验证：88 项相关测试通过；浏览器验证回车创建、来源筛选、时期切换和图表渲染，未发现 JavaScript 错误。

附件是 DSH 插件包。原生会话仍需要兼容 Desktop 的宿主适配，本次不包含 Desktop 安装程序。
