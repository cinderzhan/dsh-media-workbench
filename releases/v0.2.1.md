修复 DSH Desktop 插件安装与原生会话兼容性。

- 仓库根目录现在是真正的 DSH Bundle，包含服务端、客户端与配置入口；GitHub 源码安装和 Release 包使用同一入口。
- 宿主依赖声明为 optional peers，避免安装插件时另行下载整套 Harness。
- 原版 Desktop 无会话嵌入接口时，新建或继续绑定会话进入 DSH 原生页面，可通过「返回工作台」切回；不再要求修改 Desktop 才能使用会话。

验证：0.8.1 原始 generation 安装器成功安装；0.8.0 原版 Harness 隔离 Profile 验证插件加载、选题创建、创建与绑定会话、往返导航，无页面错误。0.8.0 / 0.8.1 核心 Harness 包和布局 API 相同，本次未完整启动 0.8.1 Electron 安装程序。

私有仓库需要先登录 GitHub 下载，解压后通过 DSH 插件管理流程安装并启用本地目录。完整操作见 README。仅复制文件不会启用插件，本次没有修改用户生产 Profile。
